{
  // LOCAL DEVELOPMENT ONLY. Nothing here is a production credential and nothing here
  // is used in production — the office deploy runs under ASPNETCORE_ENVIRONMENT=Production
  // and takes every secret from environment variables. See SECURITY-SECRETS.md.
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost;Database=AndritzVendorPortal;Trusted_Connection=True;TrustServerCertificate=True;MultipleActiveResultSets=true"
  },
  "PortalUrl": "http://localhost:5173",
  "ApiBaseUrl": "http://localhost:5000",
  "App": {
    "PathBase": ""
  },
  "JwtSettings": {
    "SecretKey": "Local-Only-Development-Signing-Key-Do-Not-Use-In-Production-000"
  },
  "Seed": {
    "DefaultAdminPassword": "LocalDev@1234"
  },
  "EmailSettings": {
    // MailHog (docker-compose) — a local sink, so no relay and no credentials.
    "Host": "localhost",
    "Port": 1025,
    "Username": "",
    "Password": "",
    "EnableSsl": false
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": {
        "Microsoft": "Information",
        "Microsoft.EntityFrameworkCore.Database.Command": "Information"
      }
    }
  }
}
